"use server"

import {
  destinations,
  activities,
  trips,
  getDestinations,
  getActivitiesByDestination,
  getActivityById,
  getTripById,
  getActivitiesByType,
} from "@/lib/data"

export { getDestinations, getActivitiesByDestination, getActivityById, getTripById, getActivitiesByType }

